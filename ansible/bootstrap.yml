
  - name: Setup local environment
    hosts: local
    become: true
    roles:
      - misc
      - git
      - xrdb
      - rxvt
      - tree
      - tmux
      - universal_ctags
      - neovim
      - python
      - ruby
      - virtualbox
      - vagrant
