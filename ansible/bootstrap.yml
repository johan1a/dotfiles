
  - name: Setup local environment
    hosts: local
    become: true
    roles:
      - misc
      - git
      - rxvt
      - tree
        #      - universal_ctags
      - python
      - ruby
      - neovim
      - vagrant
