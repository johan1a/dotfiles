
  - name: Setup local environment
    hosts: local
    become: true
    roles:
      - misc
      - git
      - rxvt
        #      - universal_ctags
      - python
      - ruby
      - neovim
      - tree
