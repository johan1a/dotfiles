
  - name: Make sure {{ user_home }} exists
    file: 
      path: "{{ user_home }}"
      state: directory

  - name: Set up local environment
    hosts: local
    become: true
    roles:
      - git
      - rxvt
      - universal_ctags
      - python
      - ruby
      - neovim
      - tree
