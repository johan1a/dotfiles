
  - name: Setup local environment
    hosts: local
    become: true
    roles:
      - misc
      - git
      - rxvt
      - tree
      - tmux
      - universal_ctags
      - neovim
      - python
      - ruby
      - virtualbox
      - vagrant
