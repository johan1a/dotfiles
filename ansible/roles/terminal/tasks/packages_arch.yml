
  - name: install termite
    pacman:
      name: "termite-git"
      state: latest

  - name: install fish
    pacman:
      name: fish
      state: latest
    register: fish

  - name: Set fish as default shell
    command: chsh -s /usr/bin/fish
    when: fish.changed
