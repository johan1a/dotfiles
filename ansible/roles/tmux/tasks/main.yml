
  - include: debian.yml
    when: debian
    tags: tmux

  - include: arch.yml
    when: arch
    tags: tmux

  - name: Link tmux config
    script: >
          scripts/install_dotfile
          "{{ dotfiles_dir }}/ansible/roles/tmux/files/tmux.conf"
          "{{ user_home }}/.tmux.conf"
    tags: tmux

  - name: Link .tmux.reset.conf
    script: >
          scripts/install_dotfile
          "{{ dotfiles_dir }}/ansible/roles/tmux/files/tmux.reset.conf"
          "{{ user_home }}/.tmux.reset.conf"
    tags: tmux
