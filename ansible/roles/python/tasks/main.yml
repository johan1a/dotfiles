
  - include: debian.yml
    when: debian

  - include: arch.yml
    when: arch

  - name: Upgrade pip3
    pip:
      name: pip
      executable: pip
      state: latest

  - name: Upgrade pip2
    pip:
      name: pip
      executable: pip2
      state: latest
    when: false

