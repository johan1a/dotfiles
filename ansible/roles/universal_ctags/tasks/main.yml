
  - include: debian.yml
    when: not lookup('env', 'CI') and not lookup('env', 'Vagrant') and ansible_os_family == "Debian"
    tags: ctags

  - include: ctags.yml
    when: not lookup('env', 'CI') and not lookup('env', 'Vagrant')
    tags: ctags
