
  - name: Install dependencies
    pacman:
      name: ['ncmpcpp', 'mpd',]
      state: latest

  - name: Install mopidy
    command: yay -S mopidy mopidy-spotify --noconfirm --needed
    become_user: "{{ username }}"
    when: false

